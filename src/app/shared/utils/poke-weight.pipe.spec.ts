import { PokeWeightPipe } from './poke-weight.pipe';

describe('PokeWeightPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeWeightPipe();
    expect(pipe).toBeTruthy();
  });
});
