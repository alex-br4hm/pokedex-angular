import { PokeHeightPipe } from './poke-height.pipe';

describe('PokeHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new PokeHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
