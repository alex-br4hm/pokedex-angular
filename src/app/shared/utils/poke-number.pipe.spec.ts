import { PokeNumberPipe } from './poke-number.pipe';

describe('PokeNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PokeNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
