import { PokeNumberPipePipe } from './poke-number-pipe.pipe';

describe('PokeNumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PokeNumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
