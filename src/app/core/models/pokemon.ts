export interface PokemonData {
  name: string;
  url: string;
}

export interface Pokemon {

}

export interface PokemonCardData {
  name: string;
  infoText: string;
  typesGer: string[];
  typesEn: string[];
  img_url: string;
  game_index: number;
}

